partitionNumber: ""
generation: ""
repoURL: https://github.com/hemant0308/argocd-platform.git
targetRevision: main

# Poll interval (seconds) for the project-groups plugin generator.
# Injected from argocd/managed via the parent ApplicationSet valuesObject.
# Override requeueAfterSeconds.projectGroups in argocd/managed/values.yaml
# to tune for all partitions at once.
requeueAfterSeconds: 30
